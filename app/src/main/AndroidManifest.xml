<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TurismoGoDPA"
        tools:targetApi="31">
        <!-- Reservar -->
        <activity
            android:name=".ui.user.reservas.ReservarActivity"
            android:exported="true">

        </activity>

        <!-- Confirmar Reserva -->
        <activity
            android:name=".ui.user.reservas.ConfirmaReservaActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".ui.InicioGeneralActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.autentication.LoginActivity"
            android:exported="false" />
    </application>

</manifest>